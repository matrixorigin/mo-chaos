apiVersion: batch/v1
kind: Job
metadata:
  labels:
    mo.chaos.data: "true"
  name: create-databases
spec:
  template:
    metadata:
      labels:
        mo.chaos.data: "true"
    spec:
      containers:
      - name: create-databases
        image: matrixorigin/mo-chaos-data:1.1.0-beta1
        env:
          - name: DURATION
            value: "120"
          - name: LOCAL_DEBUG
            value: "false"
          - name: CHAOS_JOBS
            value: "cn-pod-delete-job"
        command:
          - "python"
          - "main.py"
      restartPolicy: Never
      serviceAccountName: chaos-job-runner
  backoffLimit: 0
#  ttlSecondsAfterFinished: 5
