name: Just for Test
on:
  workflow_dispatch:
jobs:
  Run-Chaos:
    runs-on: amd64-tke-dind
    needs:
      - Wait-Until-Ready
    steps:
      - run: |
          cat ~/.kube/config
        name: Run